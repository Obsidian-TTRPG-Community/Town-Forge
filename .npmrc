# Obsidian requires the release tag to be exactly the manifest version, with no
# "v" prefix, because the update checker fetches
#   releases/download/<version>/manifest.json
# npm defaults tag-version-prefix to "v", which would produce an uninstallable
# release. Keep this file.
tag-version-prefix=""
